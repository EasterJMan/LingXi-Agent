spring:
  application:
    name: image-search-mcp-server
  profiles:
    #    active: stdio
    active: sse
server:
  port: 8127

# Pexels API（在 https://www.pexels.com/api/ 申请）
pexels:
  api-key: xxx
  api-url: https://api.pexels.com/v1/search
  per-page: 10